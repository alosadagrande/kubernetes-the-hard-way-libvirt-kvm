parameters:
 masters: 3
 numcpus: 4
 memory: 16384
 disks:
 - 50
 nets:
 - k8s-net
 cmds:
 - yum -y update

{% for num in range(0, master) %}
master0{{ num }}:
 image:  {{ image }}
 disks: {{ disks }}
 nets: {{ nets }}
 reservedns: true
 reservehost: true
 numcpus:  4
 memory: {{ memory }}
 cmds: {{ cmds }}
{% endfor %}
